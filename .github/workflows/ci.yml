name: CI

# PR-only gate; push-to-main publishes a prerelease via dev.yml.
# Pipeline body in _build.yml.

on:
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    uses: ./.github/workflows/_build.yml
    secrets: inherit
